# UniDict â€” implementation status (M0â€“M9 + Build-better track)

Progress tracker matching `PLAN.md`. Check a box off in the same PR that satisfies it.
**Per-integration detail (pattern, seams, verify lines, M7 plan) lives in [`INTEGRATIONS.md`](INTEGRATIONS.md).**
**Scope rework 2026-08-12:** full rework, not a faithful port â€” see the top of `PLAN.md`.

- T1 = JVM unit test Â· T2 = seam/fake test Â· T3 = in-game `[unidict-verify]` line.
- Legend: `[x]` done Â· `[~]` re-scoped/deferred (see PLAN) Â· `[ ]` open.
## ⚡ HEAD snapshot (2026-08-15) — supersedes the open boxes below that it touches

`feature-Implementation` has landed everything the "mod-integration" plan promised. The per-milestone
boxes below are **historical**; the **forward-looking** remaining work lives in
[`TODO.md`](TODO.md) (P0 release blockers: NEI variant hiding, `keepOneEntry` via NEI hiding, the
CoFH/TE runtime gate, docs sync).

Landed since this tracker's last full update:
- **M8 Galacticraft compressor** — implementation + tests complete (see `docs/INTEGRATIONS.md`); T3
  verify line still needs a GC-at-runtime run.
- **Forestry crates + bronze-tool recycling** — carpenter/squeezer/centrifuge plus crate-*recipe*
  wiring and bronze recycling (all non-destructive; crate-*item* creation still deferred).
- **`UnifyDrops`** — new drop-time canonicalisation module (`Config.unifyDrops()`, default on).
- **EIO / Railcraft / TE T3** — verified in the 2026-08-14 full dev-mod regression (see the M9 "Full T3 regression" entry below).



## M0 â€” Harness & risk retirement
- [x] `dependencies.gradle` finalized for kept mods; **TE = dev jar `curse.maven:ThermalExpansion-69163:2388759`** (not the API jar)
- [x] Placeholder `Mixins`/`TargetMods` enums emptied â†’ project boots with zero mixins
- [x] JUnit 5 harness wired (`addon.gradle.kts` â†’ `useJUnitPlatform`; `testImplementation` in deps)
- [x] Demo T1 test green: `TEST-â€¦SmokeTest.xml` = `tests=2 failures=0 errors=0`
- [x] `./gradlew test build` compiles main + test, Spotless/Checkstyle clean
- [x] `docs/TestPlan.md` + `docs/STATUS.md` created
- [x] Verify harness `[unidict-verify]` writer scaffolded (dev-gated; enabled via `-PunidictDevVerify` **or** the `UNIDICT_DEV_VERIFY` environment variable)
- [x] **Spike A:** static `@Accessor` on OreDictionary's private statics â€” the accessors apply cleanly, but capturing via `@Inject` into `OreDictionary.rebakeMap()` **conflicts with Hodgepodge's `SpeedupOreDictionaryTransformer`** (it ASM-rewrites `rebakeMap`, stripping injected callbacks). **Defer to Hodgepodge â€” never disable it.** The M3 seam will use a **lazy-read** bridge (call the `@Accessor` getters on demand) instead of a one-time `rebakeMap` capture. Until M3, the verify harness emits `FAIL spikeA oredict-bridge` (see Â§M3 + Â§Interop decisions).
- [x] **Spike B:** 3 TE `@Invoker("<init>")` mixins **CONFIRMED in-game** â€” `fml-client-latest.log` DEBUG shows `Recipe{Furnace,Pulverizer,Smelter}Invoker` each mixed into its TE `Recipe*` class and the `@Invoker unidict$new(â€¦)` renamed to `new$unidict_$md$â€¦` (invoker wired), **no MixinApplyError**. (Actually *constructing* a recipe through them is exercised in M7.)
- [x] M0 gate: full `runClient` boots without a mixin/NEI error (both spikes observed mid-load)

## M1 â€” Pure resource model (kinds) â€” KEEP (substrate for selection + report)
- [x] `Resource`/`UniAttributes` ported; 64-kind guard enforced (`IllegalStateException` at 64) + 10 ResourceTest T1 tests green
- [x] Pure `MetaItem` hash helpers (`pure/MetaKey`) + 5 MetaKeyTest T1 tests green; pure/resource packages contain zero `net.minecraft*` imports (grep gate PASS)

## M2 â€” Determinism & infra rework (thread pool / DI / explicit registry)
Two commits (scope decision 2026-08-13): **Commit 1** = determinism/infra (lands first, before any
integration); **Commit 2** = config surface + presets (BB-2), which also ports the config-coupled
comparators.

### Commit 1 â€” infra determinism (DONE)
- [x] Sequential `LoadStageExecutor` replaces the upstream thread pool: registration order == execution order; threads run on the calling thread (`LoadStageExecutorTest` T2: 5 tests green)
- [x] `ModuleHandler` collapsed to an ordered module list; lazy `init()` once per module (`ModuleHandlerTest` T2: 3 tests green)
- [x] Explicit `new` registry in `IntegrationModule` (currently empty â€” M6/M7 add integrations); `@Module` search / `searchForModules` deleted; `LoadStage` + `SpecifiedLoadStage` ported
- [x] DI removed: `Dependencies`/`Instantiator`/`DependenceWatcher` never recreated; dead code path gone
- [x] `Util` trimmed to `getModName` (reflection `getField`/`setField` deleted); `FixedSizeList` not used
- [x] `UniDict` wires the sequential `ModuleHandler` at FML POST_INIT + LOAD_COMPLETE
- [x] **Determinism guard** (`DeterminismGuardTest`): greps `src/main` for thread pools / reflection / deleted DI types; fails build if reintroduced
- [x] `runClient` boots cleanly with all integrations off (T3 â€” confirmed 2026-08-13; config written, no UniDict errors). Verify harness now runs (via env var) and emits one known `FAIL spikeA oredict-bridge` â€” a **Hodgepodge interop conflict**, resolved in M3 (see Â§M3).

### Commit 2 â€” config + presets (BB-2) (DONE)
- [x] `ConfigData` value object (pure, grouped: general / resources-owners / integrations) + `ConfigReader` (mapâ†’config, last-write-wins) + `ConfigPresets` (minimal / standard / max-compat), T1 tests: ConfigReaderTest 5, ConfigPresetsTest 4
- [x] `OwnerOrder` pure owner model (dedupes `enableSpecificKindSort`/`ownerOfEveryThing`/`ownerOfEvery<Kind>`): per-kind override else global; T1 OwnerOrderTest 5
- [x] Runtime `Config` reworked to load via thin `ForgeConfigIO` adapter into `ConfigData`; legacy keys accepted-but-ignored; **no config-file deletion on version mismatch** (upstream fix)
- [x] Legacy aliases: `ownerOfEveryThing`â†’`ownerPriorities`, `ownerOfEvery<Kind>`â†’`ownerOfKind.<Kind>`; removed-mod/deferred keys collected for INFO (never fatal)
- [x] `SpecificKindItemStackComparator` + `Util.itemStackComparatorByModName()` ported, driven by `OwnerOrder` (keep-one-entry black set accumulated for M4)
- [x] Config `.cfg` round-trip covered at the pure layer (fixture); forge `Configuration` glue is T3-only (rule 7 in TestPlan.md â€” needs `FMLInjectionData`)
- [x] `./gradlew build` green: 40 tests, Spotless/Checkstyle clean

## M3 â€” UniOreDictionary seam â€” KEEP, TRIMMED to read-only accessor (+ `getFirstEntry` for IE)
- [x] `IOreDictionaryAccessor` + mixin + `FakeOreDictionaryAccessor`; read-only `UniOreDictionary` reads all maps through the interface (T2 `UniOreDictionaryTest`, 6 tests)
- [x] **Spike-A carry-over:** bridge is **lazy-read** â€” `OreDictionaryMixin` implements `IOreDictionaryAccessor` (instance `@Override` methods merged onto the target, each delegating to a `private static` `@Accessor` and thus reading the live field on every call); `OreDictionaryBridge` holds a target instance cast to the interface. The one-time `@Inject` into `OreDictionary.rebakeMap()` was **removed** (Hodgepodge's `SpeedupOreDictionaryTransformer` strips it). We defer to Hodgepodge (never disable). Green line = `[unidict-verify] PASS spikeA oredict-bridge`.
- [x] Mutation methods (`removeFromElsewhere`/`keepOneEntry` collapse) NOT ported (deferred) â€” no `Util.getField`/`setField` remains anywhere in src/main
- [x] **Startup crash fixed (see below):** an early M3 attempt added `public static unidict$get*` readers to the mixin, but Mixin forbids non-private static members (`InvalidMixinException` â†’ broke `OreDictionary` load â†’ GT `NoClassDefFoundError`). Reworked to the interface-impl pattern above; `compileJava` (Mixin AP) + full build green.
- [x] `runClient` re-verified: boot clean through all 76 mods (incl. GregTech + Hodgepodge); `OreDictionaryMixin` applied with 5 accessors renamed, no `InvalidMixinException`; log shows `[unidict-verify] PASS spikeA oredict-bridge nameToId=26794` + `summary: 1 passed, 0 failed` (2026-08-13)

## M4 â€” Vertical slice (reframed): selection + vanilla furnace + report prototype â€” NO NEI hiding
- [x] Pure `SelectionRules` (keep-one-entry / NEI-hide eligibility / sort-trigger decisions) + 7 `SelectionRulesTest` T1 tests green
- [x] `MetaItem` MC glue over `pure/MetaKey` (exposes `MetaItemProvider`; `UniOreDictionary.instance()` now reuses it)
- [x] `UniResourceContainer` â€” non-destructive snapshot selection (sorts a **private copy**, never mutates forge's live OD list â€” BB-3); live-list `removeBadEntriesFromNEI`/`keepOneEntry` removed (deferred)
- [x] `ResourceHandler` (canonical-query seam) + `UniResourceHandler` (sequential `createResources`/`postInit` â€” no `parallelStream`; publishes `UniDict.resourceHandler`)
- [x] Vanilla furnace rewrite â€” `FurnaceIntegration.rewriteOutputs` **non-destructive** (only `setValue` outputs, never removes recipes / never mutates global registries) + 3 `FurnaceIntegrationTest` T2 tests green (fabricated-map)
- [x] `NEIHelper` present as the single guarded `API.hideItem` site with a dev-mode main-thread guard â€” **not invoked** in M4 (NEI hiding deferred)
- [x] First transparency report (BB-1 seed): verify pass emits `[unidict-verify] PASS resource=<name> main=<owner:registry> variants=<n>` per unified resource at load-complete (**lines sorted so the dump is diffable run-to-run**)
- [x] **In-game crash found + fixed (2026-08-13, GTNH pack):** `createResources()` was calling `Resource::register` for **every OD prefix** in the registry, blowing past the 64-kind cap (`Cannot register more than 64 resource kinds`). Since only the configured `childrenOfMetals` child kinds survive `filteredClone(childrenOfMetals)` in the metal map, we now **register bits / build containers only for `childrenOfMetals`** children â€” the taxonomy stays â‰¤ 64 and nothing the selection/report/furnace acts on is lost.
- [x] **T3 in-game gate VERIFIED (2026-08-13, GTNH pack, 76 mods):** `runClient` booted clean (no NEI crash, no `Cannot register more than 64`), `[unidict-verify] summary: 147 passed, 0 failed` â€” `PASS spikeA oredict-bridge nameToId=52267`, 145 per-resource `PASS resource=<name> main=â€¦ variants=<n>` lines (incl. `ingotIron`), and `Furnace Integration: rewrote outputs of 465 furnace recipes`.
- [ ] `./gradlew build` (Spotless/Checkstyle) on the final M4 tree â€” `test` is green (already confirmed)

## M5 - Crafting rewrite (output-only, non-destructive) - DONE

- [x] **Non-destructive output-only rewrite:** CraftingIntegration iterates CraftingManager.getRecipeList()
  and replaces each recipe's output ItemStack in place through accessor mixins -- never removes or
  rebuilds a recipe (BB-3). Covers ShapedRecipes and ShapelessRecipes (which includes Forge's
  ShapedOreRecipe/ShapelessOreRecipe via subclassing). IC2 AdvRecipe/AdvShapelessRecipe noted as a
  safe gap (metal output recipes typically use vanilla types in GTNH).
- [x] **Accessor mixins:** ShapedRecipesMixin/IShapedRecipesAccessor + ShapelessRecipesMixin/
  IShapelessRecipesAccessor (EARLY phase, remap=true for vanilla fields). Test fakes extend the real
  recipe types for instanceof compatibility.
- [x] **Config toggle:** craftingIntegration (true in standard/max-compat, false in minimal).
- [x] **Report integration:** RewriteJournal.record('crafting', 'table', rewritten) +
  [unidict-verify] PASS integration=crafting rewritten=N.
- [x] **T2 tests:** 4 CraftingIntegrationTest tests green (shaped + shapeless rewrite, unresolved
  left alone, null recipes skipped).
- [x] **Build:** ./gradlew test = 102 tests, 0 failures; Spotless/Checkstyle clean (after auto-apply).
  T3 in-game gate (dev-verify-enabled run): [unidict-verify] PASS report rewrite=crafting table rewritten=3;

## M6 â€” Machine rewrites (one PR each): AE2 Â· IC2 Â· IE Â· Chest(loot)  *(Furnace landed in M4)*
- [x] AE2 `â€¦integration=AE2` â€” **COMPLETE (2026-08-13, T3 gate).** `AE2Integration.rewriteRecipe` non-destructive (wraps each AE2 `IGrinderEntry` via a `GrinderRecipe` interface+fake seam; rewrites primary + both optional OUTPUTs to the canonical `getMainItemStack` in place â€” never `Iterator.remove()`, never `setInput()`; BB-3 + M5-deferred-respected, deviating from upstream which removed/deduped and set input). Runs at POST_INIT, early-skips on empty resource model. Tests: 3 `AE2IntegrationTest` green (no AE2 types on test classpath); `./gradlew test build` + Spotless/Checkstyle green. **T3 gate (dev-verify-enabled run):** `[unidict-verify] PASS integration=ae2 rewritten=98`, `[unidict-verify] summary: 159 passed, 0 failed`, 0 strict FAIL lines, baseline unchanged (145 `resource=` + `spikeA`, IC2 `integration=ic2 machines=10 rewritten=69` all intact); runtime `AE2 Integration: rewrote outputs of 98 AE2 grinder recipes`, no ERROR. Confirmed AE2 exposes no other unification-relevant machine: `IRegistryContainer` lists only the grinder as an output-rewrite candidate; the Inscriber's press outputs are AE2-specific with no cross-mod equivalents (matches upstream + PLAN M6).
- [x] IC2 `â€¦integration=IC2` â€” **COMPLETE (2026-08-13, T3 gate).** Non-destructive (only rebuild/setValue `RecipeOutput` item-lists via `getMainItemStack`, never removes recipes / never mutates global registries â€” BB-3) across **10** `Recipes.*` machine maps (upstream's five â€” centrifuge, metalformerRolling, blastfurance, compressor, macerator â€” plus extractor, metalformerExtruding, metalformerCutting, blockcutter, oreWashing). Built on the shared **`OutputRewriter`** core (`rewriteOutputs(map, view, resolve)` + `rewriteSingleOutputs` convenience) that **Furnace also delegates to**; per-machine dev-verify lines + summary; early-skip + null-map guard. Tests: 3 `IC2IntegrationTest` + 4 `OutputRewriterTest` + 3 `FurnaceIntegrationTest` green; `./gradlew test build` + Spotless/Checkstyle green. **T3 gate (dev-verify-enabled run):** `[unidict-verify] summary: 158 passed, 0 failed`, 11 `PASS integration=ic2 â€¦` lines (10 machines, rewritten 16+14+0+14+0+0+7+7+5+6 = **69**), 145 `PASS resource=â€¦` lines (prior lines unchanged), `PASS spikeA oredict-bridge nameToId=52267`; runtime `IC2 Integration: rewrote outputs of 69 IC2 machine recipes`, no ERROR/NPE. **History:** run-1 NPE = `Recipes.recycler.getRecipes()` returns null (recycler is a randomizer; `RecyclerRecipeManager` = `aconst_null/areturn`, verified vs IC2 2.2.828) â†’ removed recycler + defensive null-guard.
- **Et Futurum raw-ore â†’ IC2 compat â€” DEFERRED (2026-08-13), research recorded.** User paused implementation. **Confirmed from Et Futurum Requiem 2.6.52-GTNH sources** (`ModRecipes.java:416-420`): raw ores ARE registered in the OreDictionary under the standard ore tags **guarded by `ConfigFunctions.registerRawItemAsOre`** â€” `registerOre("oreCopper", rawCopper)`, `("oreIron", rawIron)`, `("oreGold", rawGold)`, plus `rawCopper`/`rawIron`/`rawGold`/`blockRaw*` tags, and modded raw ores get `ore<Metal>` too (line 1394). Since IC2's `BasicMachineRecipeManager.onOreRegister` re-caches exactly those `RecipeInputOreDict("oreX")` recipes when such a tag registers (verified via bytecode), the macerator will accept raw ores **by default** when `registerRawItemAsOre` is on â€” so a UniDict recipe-injection compat is likely **unnecessary**. **Verification not yet done in-game** for raw ores specifically; a dev-verify run with Et Futurum installed and the flag on would confirm (this needs no UniDict code). Revisit only if that run shows the macerator rejecting raw ores despite the tags.
- [x] IE `â€¦integration=IE` â€” **COMPLETE (2026-08-13; T3 gate VERIFIED).** Non-destructive rewrite of the four IE machines with cross-mod unifiable (metal) outputs â€” Arc Furnace, Blast Furnace, Crusher, Metal Press (the complete IE `api.crafting` surface that matters: Coke Oven / Bottling Machine / Blueprint Crafting are mod-specific/non-metal, intentionally untouched). IE recipe `output` fields are `final` (immutable value objects â€” verified via `javap` on the IE 2299020 dev jar), so unlike Furnace/IC2 we cannot mutate in place: each affected recipe is rebuilt with the canonical `getMainItemStack` and replaced **at its index** (`List.set`) preserving count + order â€” never `Iterator.remove`, never a removal (BB-3). Built on a new shared `OutputRewriter.rewriteList(list, view, resolve)` list-backing of the existing core (`mapItems`/`isChanged` helpers shared with `rewriteOutputs`). Crusher keeps public non-final `secondaryOutput`/`secondaryChance` (no `UniCrusherRecipe` subclass); Metal Press iterates the `ArrayListMultimap` by mold key via `ListMultimap.get(key)`; Arc furnace preserves `specialRecipeType` (upstream dropped it); Metal Press preserves a non-default `inputSize`. Dropped upstream's `getFirstEntry` input lookup + `uniques` dedup (they existed only because upstream removed+re-added; input rewriting = M5-deferred). Tests: 2 new `OutputRewriterTest` cases (`rewriteList` count-preserving + unchanged-identity) + existing 4 green; `./gradlew test build` + Spotless (auto-applied)/Checkstyle green. **T3 gate VERIFIED (2026-08-13, GTNH pack run):** `[unidict-verify] summary: 164 passed, 0 failed`; `PASS integration=ie` lines â€” `machine=arcFurnace rewritten=12`, `machine=blastFurnace rewritten=2`, `machine=crusher rewritten=1`, `machine=metalPress rewritten=4`, `machines=4 rewritten=19` â€” match the runtime `IE Integration: rewrote outputs of 19 IE machine recipes`. The single `*FAIL` grep hit is a false positive (the word "failed" in `summary: â€¦ 0 failed`); strict `unidict-verify] FAIL ` = 0 matches. Prior lines unchanged (Furnace 465, IC2 69, AE2 98, `spikeA oredict-bridge nameToId=55317`, `resource report resources=145`). No UniDict exception/MixinApplyError (remaining IC2 `reactorMOX` ParseExceptions + `ClassNotFoundException`s are pre-existing non-UniDict pack noise).
- [x] Chest `â€¦integration=Chest` â€” **COMPLETE (2026-08-13).** Loot rewrite (`ChestIntegration`, "best-effort, minor"): rewrites the ITEM of every Forge chest-loot entry (each `WeightedRandomChestContent` the game can drop from dungeons/mineshafts/villages/strongholds/bonus chests) in place to the canonical `getMainItemStack` â€” non-destructive (BB-3): only the entry's item reference changes via the accessor seam, so the `contents` list keeps its exact count/order and each entry keeps its weights/min/max. Introduces the plan's **first accessor seam** (`ChestGenHooksMixin` @Accessor&times;2: static `chestInfo` registry + instance `contents`), plus a **documented 1.7.10-necessitated second accessor** (`WeightedRandomChestContentMixin`): upstream (1.12.2) rewrote `theItemId` as a *public* field directly â€” in MC 1.7.10 it is `private` (notch `qx.b`, SRG `field_76297_b`, verified via fields.csv + javap), so we expose get+set via a second @Accessor mixin (remap=**true**, vanilla field; the ChestGenHooks fields are remap=false Forge-added). Interfaces `IChestGenHooksAccessor` + `IWeightedRandomChestContentAccessor` (plain, rule 6); fakes in test sources. Registered early via `Mixins.CHEST_GEN`; wired in `IntegrationModule`. Tests: 3 `ChestIntegrationTest` (fake-driven `rewriteContents`/`rewriteCategory`/`rewriteContent`) green; `./gradlew test build` + Spotless/Checkstyle green (16 suites, 0 fail). Runs at POST_INIT (default), early-skips on empty resource model; config-gated `Config.chest()`. **Toolchain catches (AP vs runtime):** (1) the Mixin **AP** requires an `@Accessor` stub to return the *exact* field descriptor (`HashMap`/`ArrayList`, not `Map`/`List`) or it fails `Could not locate @Accessor target â€¦`; (2) the Mixin **transformer** rejects a *non-abstract instance* `@Accessor` at apply time (`InvalidAccessorException: @Accessor method accessor$contents is not abstract` â€” the run-1 7:50 PM crash while applying `ChestGenHooksMixin`), so every instance accessor is `protected abstract`, while the static `chestInfo` accessor stays a concrete `private static` method (the proven M3 `OreDictionaryMixin` pattern â€” Java forbids `abstract static`). **T3 gate VERIFIED (2026-08-13, GTNH pack run):** clean boot (no crash; the 7:50 PM `ChestGenHooksMixin` apply crash is fixed); `[unidict-verify] PASS integration=Chest rewritten=38`; `[unidict-verify] summary: 165 passed, 0 failed` (IE run was 164; the new `integration=Chest` line is the only delta); prior lines unchanged (IC2 machines=10 rewritten=69, AE2 rewritten=98, IE machines=4 rewritten=19, `spikeA oredict-bridge nameToId=55317`, `resource report resources=145`); runtime `Chest Integration: rewrote 38 chest loot entries to their canonical items`.

## M7 â€” Machine rewrites (accessor/mixin): EIO Â· Railcraft Â· TE + Forestry
- [x] **Source: EIO** `â€¦integration=EIO` â€” `EIOIntegration` (alloy smelter + SAG mill via `OutputRewriter.rewriteList`, `List.set`) + `OreDictionaryPreferencesMixin`/`IOreDictionaryPreferencesAccessor` (clears EIO's `preferences` so it yields the canonical entry). Registered early via `Mixins.ENDER_IO` + `TargetMods.ENDER_IO`, wired in `IntegrationModule` (`Config.enderIO() && Loader.isModLoaded`). **Design note:** the two EIO `OutputView`s are lazy `Supplier`s so `EIOIntegration.<clinit>` never resolves Ender IO's classes â€” required so the package-private seams stay T2-reachable on a test classpath without Ender IO (see Â§EIO below). Tests: 4 `EIOIntegrationTest` green (fake-driven `rewriteRecipes` seam + `fixOreDictPreferences` via `FakeOreDictionaryPreferencesAccessor`).
- [x] **Source: Railcraft** `â€¦integration=Railcraft` â€” `RailcraftIntegration` (**blast furnace** via `OutputRewriter.rewriteList`, `List.set` + `BlastFurnaceCraftingManagerMixin`/`IBlastFurnaceCraftingManagerAccessor` for the private `recipes`; **rock crusher** via `OutputRewriter.rewriteChanceOutputs` â€” multi-output + per-output chance, rewritten **in place** through the *public* `RockCrusherCraftingManager.getRecipes()`/`IRockCrusherRecipe#getOutputs()`, which needs no mixin). Both non-destructive (BB-3), no recipe removed. Registered via `Mixins.RAILCRAFT` (blast-furnace accessor only) + `TargetMods.RAILCRAFT`, wired in `IntegrationModule`. Tests: 5 `RailcraftIntegrationTest` green (fake-driven `rewriteRecipes` seam via `FakeBlastFurnaceCraftingManagerAccessor` + `rewriteCrusherOutputs` seam via a neutral `CrushRecipe` holder).
- [x] **Source: TE** `â€¦integration=ThermalExpansion` â€” `TEIntegration` (redstone furnace / pulverizer / induction smelter via `OutputRewriter.rewriteOutputs`, `Map.setValue`; rebuilt through the M0-Spike-B `@Invoker`s via the `IRecipeFurnaceFactory`/`IRecipePulverizerFactory`/`IRecipeSmelterFactory` interface seams) + 3 accessor mixins (`FurnaceManagerMixin`/`PulverizerManagerMixin`/`SmelterManagerMixin`, each `recipeMap` `private static` `@Accessor`) + 3 accessor interfaces. Runs at `@SpecifiedLoadStage(LOAD_COMPLETE)`. Registered via `Mixins.THERMAL_EXPANSION`, wired in `IntegrationModule`. Tests: 2 `TEIntegrationTest` green (fake-driven `rewriteOutputs` seam via `FakeFurnaceManagerAccessor`/`FakePulverizerManagerAccessor`/`FakeSmelterManagerAccessor`).
- [x] **Source: Forestry** `…integration=Forestry` — `ForestryIntegration` (non-destructive BB-3): **carpenter** grid outputs rewritten in place via the early `ShapedOreRecipeMixin`/`IShapedOreRecipeAccessor` (`ShapedOreRecipe.output`, `remap=false`) — the unmodifiable `RecipeManagers.carpenterManager.recipes()` collection is only iterated; **squeezer** container-recipe remnants rewritten via `Map.Entry.setValue` on the public `SqueezerRecipeManager.containerRecipes` (no mixin) — key/count preserved; **centrifuge** product keys rewritten in place (clear+putAll) via the late `CentrifugeRecipeMixin`/`ICentrifugeRecipeAccessor` (`CentrifugeRecipe.outputs`, `TargetMods.FORESTRY`) — what `getProducts(Random)` rolls, so a bee-comb → metal output unifies. Wired in `IntegrationModule` (`Config.forestry() && Loader.isModLoaded`). **Also adds (not rewrites):** bronze-tool recycling + crate-*recipe* wiring through Forestry's supported `RecipeManagers.carpenterManager.addRecipe` via a neutral `ICarpenterRecipeAdder` seam (bronze: broken pickaxe → 2 × canonical ingot / shovel → 1 ×; crates: 9 × `ingot<Name>` → crate + crate → 9 × canonical ingot, only for already-registered `Forestry:crated<Name>` items). **Scoped out** (documented): fluid outputs (no 1.7.10 fluid-equivalence model), crate-*item* creation (fragile runtime `ItemCrated`, deferred). **T3 (in-game, 2026-08-15):** `PASS integration=Forestry machine=carpenter rewritten=7`, `machine=squeezer rewritten=1`, `machine=centrifuge rewritten=0`, `machines=3 rewritten=8`; `CentrifugeRecipeMixin` mixed cleanly into `CentrifugeRecipe`; strict FAIL 0, summary `212 passed, 0 failed`, no UniDict error. Tests: 15 `ForestryIntegrationTest` green (fake accessor + neutral `Holder`/map seams + `FakeCarpenterRecipeAdder` for the bronze/crate additions; no Forestry types on the test classpath); `./gradlew test build` + Spotless/Checkstyle green.
- [x] **T2 tests green** â€” 5 accessor fakes + 3 integration test classes (EIO 4 / Railcraft 4 / TE 2) all pass (`./gradlew test` = 86 tests, 0 failures; Spotless/Checkstyle clean). The EIO `fixOreDictPreferences` seam was widened to accept `IOreDictionaryPreferencesAccessor` for testability (mirrors Chest's `rewriteCategory(accessor, â€¦)`).
- [x] **T3 in-game verify â€” EIO âœ…, Railcraft âœ…, TE BROKEN then fixed.** A full `runClient` (all kept mods) confirmed the EIO and Railcraft verify lines PASS, but **TE failed fatally**: `LoaderException: NoClassDefFoundError: â€¦mixins.late.TERecipeFactory` / `IllegalClassLoadError: â€¦ is in a defined mixin package â€¦ cannot be referenced directly`. The wrapper `TERecipeFactory` lived in the closed mixin package `mixins.late` and could not be referenced from the non-mixin `TEIntegration`. **Fix:** surfaces the TE `@Invoker`s through the interface-on-target pattern exactly like the accessors â€” each `@Invoker` mixin now implements an `IRecipe*Factory` interface (in `com.mrfuzzihead.unidict.te`, non-mixin) and `TEIntegration` casts the recipe value object to it; the `@Invoker` is invoked inside the mixin package (legal). `TERecipeFactory` deleted. **Re-run `runClient` to reconfirm `PASS integration=TE machines=3` now CONFIRMED â€” 2026-08-14 M9 regression: `machines=3 rewritten=253`, no `NoClassDefFoundError`/`IllegalClassLoadError`.**

## M8 â€” API/helper surface â€” MOSTLY DEFERRED (~)
- [ ] Only the minimal read surface kept integrations actually use

## M9 â€” Cleanup sweep + full regression
- [x] **Sweep:** no dead-code stragglers remain in `src/main` â€” a grep for `FixedSizeList`/`Dependencies`/`DependenceWatcher`/`Instantiator`/`searchForModules`/`@Module`/`getField`/`setField`/removed-mod ids hits only javadoc/comments describing their removal, never live code
- [x] **Galacticraft** — the deferred stub is now implemented as the compressor rewrite (this work): GC dep enabled in `dependencies.gradle` with `exclude module: "GT5-Unofficial"` (+ TinkersConstruct) so GregTech never reaches the dev classpath; `ShapelessOreRecipeMixin` added (early, `Mixins.GALACTICRAFT`) for the shapeless compressor path; `ConfigData`/`ConfigReader`/`ConfigPresets`/`ForgeConfigIO`/`Config` gained the `galacticraftIntegration` toggle (off in `minimal`, on in `standard`/`maxCompat`); `GalacticraftIntegration` rewrites `CompressorRecipes` outputs in place (BB-3) — see `INTEGRATIONS.md §Galacticraft`
- [x] **Load-order corrections (T3, 2026-08-15):** Galacticraft registers its configurable compressor recipes only at `FMLServerStarting` (a `rewritten=0` at POST_INIT confirmed), so the compressor rewrite now runs from `UniDict.serverStarted` → `IntegrationModule.runGalacticraftCompressor()` (not a `LoadStage` thread). And the **vanilla Furnace rewrite moved from POST_INIT → `LOAD_COMPLETE`**: it reads the global `FurnaceRecipes` map consumed by vanilla / IC2 / GC electric+arc furnaces, and POST_INIT missed late-registered smelting (Et Futurum raw-ore/raw-copper → non-priority ingot). At `LOAD_COMPLETE` those now resolve to the priority (Thermal Foundation) ingot.
- [x] `./gradlew build` green: 19 test classes / **79 tests, 0 failures** (incl. `DeterminismGuardTest`); Spotless + Checkstyle clean
- [x] **Full T3 regression (2026-08-14, full dev mod set):** `runClient -PunidictDevVerify` â†’ `[unidict-verify] summary: 124 passed, 0 failed`; 0 `unidict-verify] FAIL` matches; 0 mixin/fatal matches. Per-integration all PASS: IC2 `machines=10 rewritten=75`, AE2 `rewritten=61`, Chest `rewritten=38`, EIO `machines=2 rewritten=0`, Railcraft `rewritten=0`, TE `machines=3 rewritten=253`; `spikeA oredict-bridge nameToId=15489`; resource report `resources=105`. (*Evidence path: `build/runclient-log.txt`; the `[unidict-verify]` lines went to the console appender, not `run/client/logs/latest.log` â€” see note below.*)
- [x] **Eyeball pass:** only errors in the run are cosmetic AE2 block-texture PNG warnings (missing assets, pre-existing) â€” no UniDict or mixin errors
- [x] `git log --oneline` reads as the milestone list: `â€¦ M0â†’M2â†’M4â†’M6â†’M7 complete` (M3/M5 landed within those commits)

## Build-better track (`PLAN.md` has detail + gates)
- [x] **BB-1 Transparency** â€” `/unidict report`: per-resource canonical entry, variants, owners, what got rewritten
  - Implemented as a **dev + user-facing** feature (docs/PLAN.md Â§BB-1). Ships: a **`/unidict report`** command
    (`CommandUniDict`, registered at `FMLServerStartingEvent` in `CommonProxy`) and the productionized
    `[unidict-verify] report â€¦` lines from the M4 seed.
  - **Model:** pure `report` package (`ReportEntry` + `ReportFormatter` + `RewriteRecord`, zero
    `net.minecraft*`) computes/renders the report; MC-thin `UniDictReport` resolves `ItemStack`s to
    qualified names; runtime **`RewriteJournal`** captures "what got rewritten" per integration/machine
    (integrations record on every run, independent of the dev verify switch, so the user command works
    in a normal build). Lines are sorted so the dump is stable + diffable run-to-run.
  - **Every kept rewrite has a matching `report rewrite=<source> <machine> rewritten=<n>` line:** furnace,
    ae2 grinder, chest loot, each IC2 machine, each IE machine, EIO alloy/sag, Railcraft blast furnace
    + rock crusher,
    each TE machine â€” each records into the journal.
  - **Tests:** T1 `ReportFormatterTest` (4 tests) on pure entry sorting/owner-derivation + line/summary
    formatting (docs/TestPlan.md rule 2). `./gradlew build` green incl. Spotless + Checkstyle.
  - **T3 gate â€” VERIFIED (2026-08-14, `fml-client-latest.log`):** `[unidict-verify] PASS report resource=â€¦ main=â€¦ variants=â€¦ owners=â€¦` Ã— **105** (e.g. `ingotCopper main=ThermalFoundation:material variants=7 owners=Forestry,IC2,ImmersiveEngineering,Railcraft,TConstruct,ThermalFoundation,etfuturum`); `PASS report rewrite=â€¦` Ã— 16 (furnace=64, ae2=61, chest=38, eio alloy+sag=0, ic2 Ã—10 incl. macerator=16/centrifuge=20/compressor=14/metalformerRolling=7/blockcutter=7/blastfurance=5/oreWashing=6, railcraft=0); `PASS report summary=resources=105 rewrites=16`; harness **`summary: 246 passed, 0 failed`**, 0 `unidict-verify] FAIL` matches.
  - **T3 ordering fix (2026-08-14):** first dev run showed the LOAD_COMPLETE-stage **TE** integration (`machines=3 rewritten=253`) recording its journal entries *after* the report was already emitted â€” because `UniDict.loadComplete()` ran `proxy.loadComplete()` (verify/report) before `moduleHandler.startModules(LOAD_COMPLETE)` (TE). Reordered so LOAD_COMPLETE modules run before the verify pass; TE now appears in the report. (`ImmersiveEngineering` was loaded in that run but produced no `integration=ie` lines because `Config.ie()` was off in the run's config â€” correct, nothing to rewrite.)
- [x] **BB-2 Config presets** â€” grouped categories; minimal / standard / max-compat
  - The pure substrate landed in **M2 commit 2** (grouped `ConfigData`, `ConfigPresets`, `ConfigReader`
    last-write-wins, `OwnerOrder`, legacy aliases; T1 tests green). This BB-2 milestone adds the last
    missing piece â€” a **user-facing runtime preset selector** so the presets are actually reachable:
    - `ConfigPresets.byName(...)` resolver (case-insensitive `minimal` / `standard` / `max-compat`,
      unknown/blank â†’ `standard` so a typo never yields an empty surface) + `DEFAULT_NAME` constant.
    - `ForgeConfigIO.readPreset(cfg)` + the `preset` key registered in the `.cfg`'s *general* category;
      `ConfigReader` recognises the key (consumed by the loader, never reported "ignored").
    - `Config.load()` now reads the selected preset **first**, then registers defaults **and** parses
      against that preset's `ConfigData` â€” so *"presets pick defaults; explicit keys still override"*
      holds at runtime, not just at the pure layer.
  - **Tests:** ConfigPresetsTest (now 5) adds `byName` resolution; ConfigReaderTest (now 7) adds
    preset-key-not-ignored + non-standard-preset-as-default-surface (explicit key still wins);
    OwnerOrderTest (now 7) adds the unlisted-tie tiebreak coverage.
  - **Owner tiebreak (2026-08-14):** unlisted mods all share `OwnerOrder.NOT_LISTED`, so when no listed
    mod holds a resource the canonical entry was previously chosen by sort instability. `OwnerOrder`
    now breaks equal-`NOT_LISTED` ranks **deterministically** via a lexical (null-safe) mod-name
    comparison â€” added to **both** `compare` (per-kind path) and the new `compareGlobal` (the default
    global path), which `Util.itemStackComparatorByModName()` now delegates to. This keeps the BB-1
    report's `main=â€¦` value stable + diffable run-to-run even for resources owned only by unlisted mods.
  - `./gradlew test build` green (91 tests, 0 failures); Spotless/Checkstyle clean.
  - **Caveat (documented in the config comment):** loading is additive â€” the `.cfg` keeps any keys the
    mod previously wrote, and a present file value is treated as an explicit override. So switching the
    `preset` line on an *existing* config applies to keys that don't already carry a value in the file;
    a fresh (or regenerated) config surface is fully preset-driven. This preserves the M2 "never wipe
    user settings" guarantee.
- [ ] **BB-3 Non-destructive rewriting** â€” outputs-only rewrites; grep guard; fabricated-map T2 test
- [ ] **BB-4 Broader equivalence** â€” â‰¥1 non-OD equivalence class implemented, tested, reported
  - **Scope decision (2026-08-13):** fuels/coke is the first non-metal equivalence class â€” **deferred but prioritized (next build-better milestone, not M6/M7).** Motivated by the pack genuinely wanting single stackable coal coke (Railcraft + IE both emit `fuelCoke`). Engine is already resource-agnostic (`getMainItemStack`), so this needs (1) a new kind for the fuel/coke form in the M1 taxonomy (within the 64-kind guard), (2) a config surface (resurrect the deferred `customUnifiedResources` / a "fuels" preset toggle), and (3) a **fuel-equivalence classifier** that preserves per-item burn time / exact-item-check semantics â€” NOT naive OD tag-equality (metals are near-interchangeable; fuels are not). Prior to implementation, verify in the live GTNH pack that Railcraft/IE coke actually share a tag and what each fuel value is. Add a verify/report line + T3 gate (BB-4 gate: "â‰¥1 non-OD equivalence class implemented, tested, reported").
- [~] Reload / re-run module â€” deferred
