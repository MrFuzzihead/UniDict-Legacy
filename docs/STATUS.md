# UniDict — implementation status (M0–M9 + Build-better track)

Progress tracker matching `PLAN.md`. Check a box off in the same PR that satisfies it.
**Per-integration detail (pattern, seams, verify lines, M7 plan) lives in [`INTEGRATIONS.md`](INTEGRATIONS.md).**
**Scope rework 2026-08-12:** full rework, not a faithful port — see the top of `PLAN.md`.

- T1 = JVM unit test · T2 = seam/fake test · T3 = in-game `[unidict-verify]` line.
- Legend: `[x]` done · `[~]` re-scoped/deferred (see PLAN) · `[ ]` open.

## M0 — Harness & risk retirement
- [x] `dependencies.gradle` finalized for kept mods; **TE = dev jar `curse.maven:ThermalExpansion-69163:2388759`** (not the API jar)
- [x] Placeholder `Mixins`/`TargetMods` enums emptied → project boots with zero mixins
- [x] JUnit 5 harness wired (`addon.gradle.kts` → `useJUnitPlatform`; `testImplementation` in deps)
- [x] Demo T1 test green: `TEST-…SmokeTest.xml` = `tests=2 failures=0 errors=0`
- [x] `./gradlew test build` compiles main + test, Spotless/Checkstyle clean
- [x] `docs/TestPlan.md` + `docs/STATUS.md` created
- [x] Verify harness `[unidict-verify]` writer scaffolded (dev-gated; enabled via `-PunidictDevVerify` **or** the `UNIDICT_DEV_VERIFY` environment variable)
- [~] **Spike A:** static `@Accessor` on OreDictionary's private statics — the accessors apply cleanly, but capturing via `@Inject` into `OreDictionary.rebakeMap()` **conflicts with Hodgepodge's `SpeedupOreDictionaryTransformer`** (it ASM-rewrites `rebakeMap`, stripping injected callbacks). **Defer to Hodgepodge — never disable it.** The M3 seam will use a **lazy-read** bridge (call the `@Accessor` getters on demand) instead of a one-time `rebakeMap` capture. Until M3, the verify harness emits `FAIL spikeA oredict-bridge` (see §M3 + §Interop decisions).
- [x] **Spike B:** 3 TE `@Invoker("<init>")` mixins **CONFIRMED in-game** — `fml-client-latest.log` DEBUG shows `Recipe{Furnace,Pulverizer,Smelter}Invoker` each mixed into its TE `Recipe*` class and the `@Invoker unidict$new(…)` renamed to `new$unidict_$md$…` (invoker wired), **no MixinApplyError**. (Actually *constructing* a recipe through them is exercised in M7.)
- [x] M0 gate: full `runClient` boots without a mixin/NEI error (both spikes observed mid-load)

## M1 — Pure resource model (kinds) — KEEP (substrate for selection + report)
- [x] `Resource`/`UniAttributes` ported; 64-kind guard enforced (`IllegalStateException` at 64) + 10 ResourceTest T1 tests green
- [x] Pure `MetaItem` hash helpers (`pure/MetaKey`) + 5 MetaKeyTest T1 tests green; pure/resource packages contain zero `net.minecraft*` imports (grep gate PASS)

## M2 — Determinism & infra rework (thread pool / DI / explicit registry)
Two commits (scope decision 2026-08-13): **Commit 1** = determinism/infra (lands first, before any
integration); **Commit 2** = config surface + presets (BB-2), which also ports the config-coupled
comparators.

### Commit 1 — infra determinism (DONE)
- [x] Sequential `LoadStageExecutor` replaces the upstream thread pool: registration order == execution order; threads run on the calling thread (`LoadStageExecutorTest` T2: 5 tests green)
- [x] `ModuleHandler` collapsed to an ordered module list; lazy `init()` once per module (`ModuleHandlerTest` T2: 3 tests green)
- [x] Explicit `new` registry in `IntegrationModule` (currently empty — M6/M7 add integrations); `@Module` search / `searchForModules` deleted; `LoadStage` + `SpecifiedLoadStage` ported
- [x] DI removed: `Dependencies`/`Instantiator`/`DependenceWatcher` never recreated; dead code path gone
- [x] `Util` trimmed to `getModName` (reflection `getField`/`setField` deleted); `FixedSizeList` not used
- [x] `UniDict` wires the sequential `ModuleHandler` at FML POST_INIT + LOAD_COMPLETE
- [x] **Determinism guard** (`DeterminismGuardTest`): greps `src/main` for thread pools / reflection / deleted DI types; fails build if reintroduced
- [x] `runClient` boots cleanly with all integrations off (T3 — confirmed 2026-08-13; config written, no UniDict errors). Verify harness now runs (via env var) and emits one known `FAIL spikeA oredict-bridge` — a **Hodgepodge interop conflict**, resolved in M3 (see §M3).

### Commit 2 — config + presets (BB-2) (DONE)
- [x] `ConfigData` value object (pure, grouped: general / resources-owners / integrations) + `ConfigReader` (map→config, last-write-wins) + `ConfigPresets` (minimal / standard / max-compat), T1 tests: ConfigReaderTest 5, ConfigPresetsTest 4
- [x] `OwnerOrder` pure owner model (dedupes `enableSpecificKindSort`/`ownerOfEveryThing`/`ownerOfEvery<Kind>`): per-kind override else global; T1 OwnerOrderTest 5
- [x] Runtime `Config` reworked to load via thin `ForgeConfigIO` adapter into `ConfigData`; legacy keys accepted-but-ignored; **no config-file deletion on version mismatch** (upstream fix)
- [x] Legacy aliases: `ownerOfEveryThing`→`ownerPriorities`, `ownerOfEvery<Kind>`→`ownerOfKind.<Kind>`; removed-mod/deferred keys collected for INFO (never fatal)
- [x] `SpecificKindItemStackComparator` + `Util.itemStackComparatorByModName()` ported, driven by `OwnerOrder` (keep-one-entry black set accumulated for M4)
- [x] Config `.cfg` round-trip covered at the pure layer (fixture); forge `Configuration` glue is T3-only (rule 7 in TestPlan.md — needs `FMLInjectionData`)
- [x] `./gradlew build` green: 40 tests, Spotless/Checkstyle clean

## M3 — UniOreDictionary seam — KEEP, TRIMMED to read-only accessor (+ `getFirstEntry` for IE)
- [x] `IOreDictionaryAccessor` + mixin + `FakeOreDictionaryAccessor`; read-only `UniOreDictionary` reads all maps through the interface (T2 `UniOreDictionaryTest`, 6 tests)
- [x] **Spike-A carry-over:** bridge is **lazy-read** — `OreDictionaryMixin` implements `IOreDictionaryAccessor` (instance `@Override` methods merged onto the target, each delegating to a `private static` `@Accessor` and thus reading the live field on every call); `OreDictionaryBridge` holds a target instance cast to the interface. The one-time `@Inject` into `OreDictionary.rebakeMap()` was **removed** (Hodgepodge's `SpeedupOreDictionaryTransformer` strips it). We defer to Hodgepodge (never disable). Green line = `[unidict-verify] PASS spikeA oredict-bridge`.
- [x] Mutation methods (`removeFromElsewhere`/`keepOneEntry` collapse) NOT ported (deferred) — no `Util.getField`/`setField` remains anywhere in src/main
- [x] **Startup crash fixed (see below):** an early M3 attempt added `public static unidict$get*` readers to the mixin, but Mixin forbids non-private static members (`InvalidMixinException` → broke `OreDictionary` load → GT `NoClassDefFoundError`). Reworked to the interface-impl pattern above; `compileJava` (Mixin AP) + full build green.
- [x] `runClient` re-verified: boot clean through all 76 mods (incl. GregTech + Hodgepodge); `OreDictionaryMixin` applied with 5 accessors renamed, no `InvalidMixinException`; log shows `[unidict-verify] PASS spikeA oredict-bridge nameToId=26794` + `summary: 1 passed, 0 failed` (2026-08-13)

## M4 — Vertical slice (reframed): selection + vanilla furnace + report prototype — NO NEI hiding
- [x] Pure `SelectionRules` (keep-one-entry / NEI-hide eligibility / sort-trigger decisions) + 7 `SelectionRulesTest` T1 tests green
- [x] `MetaItem` MC glue over `pure/MetaKey` (exposes `MetaItemProvider`; `UniOreDictionary.instance()` now reuses it)
- [x] `UniResourceContainer` — non-destructive snapshot selection (sorts a **private copy**, never mutates forge's live OD list — BB-3); live-list `removeBadEntriesFromNEI`/`keepOneEntry` removed (deferred)
- [x] `ResourceHandler` (canonical-query seam) + `UniResourceHandler` (sequential `createResources`/`postInit` — no `parallelStream`; publishes `UniDict.resourceHandler`)
- [x] Vanilla furnace rewrite — `FurnaceIntegration.rewriteOutputs` **non-destructive** (only `setValue` outputs, never removes recipes / never mutates global registries) + 3 `FurnaceIntegrationTest` T2 tests green (fabricated-map)
- [x] `NEIHelper` present as the single guarded `API.hideItem` site with a dev-mode main-thread guard — **not invoked** in M4 (NEI hiding deferred)
- [x] First transparency report (BB-1 seed): verify pass emits `[unidict-verify] PASS resource=<name> main=<owner:registry> variants=<n>` per unified resource at load-complete (**lines sorted so the dump is diffable run-to-run**)
- [x] **In-game crash found + fixed (2026-08-13, GTNH pack):** `createResources()` was calling `Resource::register` for **every OD prefix** in the registry, blowing past the 64-kind cap (`Cannot register more than 64 resource kinds`). Since only the configured `childrenOfMetals` child kinds survive `filteredClone(childrenOfMetals)` in the metal map, we now **register bits / build containers only for `childrenOfMetals`** children — the taxonomy stays ≤ 64 and nothing the selection/report/furnace acts on is lost.
- [x] **T3 in-game gate VERIFIED (2026-08-13, GTNH pack, 76 mods):** `runClient` booted clean (no NEI crash, no `Cannot register more than 64`), `[unidict-verify] summary: 147 passed, 0 failed` — `PASS spikeA oredict-bridge nameToId=52267`, 145 per-resource `PASS resource=<name> main=… variants=<n>` lines (incl. `ingotIron`), and `Furnace Integration: rewrote outputs of 465 furnace recipes`.
- [ ] `./gradlew build` (Spotless/Checkstyle) on the final M4 tree — `test` is green (already confirmed)

## M5 — Crafting rewrite + recipe-key — DEFERRED (~)

## M6 — Machine rewrites (one PR each): AE2 · IC2 · IE · Chest(loot)  *(Furnace landed in M4)*
- [x] AE2 `…integration=AE2` — **COMPLETE (2026-08-13, T3 gate).** `AE2Integration.rewriteRecipe` non-destructive (wraps each AE2 `IGrinderEntry` via a `GrinderRecipe` interface+fake seam; rewrites primary + both optional OUTPUTs to the canonical `getMainItemStack` in place — never `Iterator.remove()`, never `setInput()`; BB-3 + M5-deferred-respected, deviating from upstream which removed/deduped and set input). Runs at POST_INIT, early-skips on empty resource model. Tests: 3 `AE2IntegrationTest` green (no AE2 types on test classpath); `./gradlew test build` + Spotless/Checkstyle green. **T3 gate (dev-verify-enabled run):** `[unidict-verify] PASS integration=ae2 rewritten=98`, `[unidict-verify] summary: 159 passed, 0 failed`, 0 strict FAIL lines, baseline unchanged (145 `resource=` + `spikeA`, IC2 `integration=ic2 machines=10 rewritten=69` all intact); runtime `AE2 Integration: rewrote outputs of 98 AE2 grinder recipes`, no ERROR. Confirmed AE2 exposes no other unification-relevant machine: `IRegistryContainer` lists only the grinder as an output-rewrite candidate; the Inscriber's press outputs are AE2-specific with no cross-mod equivalents (matches upstream + PLAN M6).
- [x] IC2 `…integration=IC2` — **COMPLETE (2026-08-13, T3 gate).** Non-destructive (only rebuild/setValue `RecipeOutput` item-lists via `getMainItemStack`, never removes recipes / never mutates global registries — BB-3) across **10** `Recipes.*` machine maps (upstream's five — centrifuge, metalformerRolling, blastfurance, compressor, macerator — plus extractor, metalformerExtruding, metalformerCutting, blockcutter, oreWashing). Built on the shared **`OutputRewriter`** core (`rewriteOutputs(map, view, resolve)` + `rewriteSingleOutputs` convenience) that **Furnace also delegates to**; per-machine dev-verify lines + summary; early-skip + null-map guard. Tests: 3 `IC2IntegrationTest` + 4 `OutputRewriterTest` + 3 `FurnaceIntegrationTest` green; `./gradlew test build` + Spotless/Checkstyle green. **T3 gate (dev-verify-enabled run):** `[unidict-verify] summary: 158 passed, 0 failed`, 11 `PASS integration=ic2 …` lines (10 machines, rewritten 16+14+0+14+0+0+7+7+5+6 = **69**), 145 `PASS resource=…` lines (prior lines unchanged), `PASS spikeA oredict-bridge nameToId=52267`; runtime `IC2 Integration: rewrote outputs of 69 IC2 machine recipes`, no ERROR/NPE. **History:** run-1 NPE = `Recipes.recycler.getRecipes()` returns null (recycler is a randomizer; `RecyclerRecipeManager` = `aconst_null/areturn`, verified vs IC2 2.2.828) → removed recycler + defensive null-guard.
- **Et Futurum raw-ore → IC2 compat — DEFERRED (2026-08-13), research recorded.** User paused implementation. **Confirmed from Et Futurum Requiem 2.6.52-GTNH sources** (`ModRecipes.java:416-420`): raw ores ARE registered in the OreDictionary under the standard ore tags **guarded by `ConfigFunctions.registerRawItemAsOre`** — `registerOre("oreCopper", rawCopper)`, `("oreIron", rawIron)`, `("oreGold", rawGold)`, plus `rawCopper`/`rawIron`/`rawGold`/`blockRaw*` tags, and modded raw ores get `ore<Metal>` too (line 1394). Since IC2's `BasicMachineRecipeManager.onOreRegister` re-caches exactly those `RecipeInputOreDict("oreX")` recipes when such a tag registers (verified via bytecode), the macerator will accept raw ores **by default** when `registerRawItemAsOre` is on — so a UniDict recipe-injection compat is likely **unnecessary**. **Verification not yet done in-game** for raw ores specifically; a dev-verify run with Et Futurum installed and the flag on would confirm (this needs no UniDict code). Revisit only if that run shows the macerator rejecting raw ores despite the tags.
- [x] IE `…integration=IE` — **COMPLETE (2026-08-13; T3 gate VERIFIED).** Non-destructive rewrite of the four IE machines with cross-mod unifiable (metal) outputs — Arc Furnace, Blast Furnace, Crusher, Metal Press (the complete IE `api.crafting` surface that matters: Coke Oven / Bottling Machine / Blueprint Crafting are mod-specific/non-metal, intentionally untouched). IE recipe `output` fields are `final` (immutable value objects — verified via `javap` on the IE 2299020 dev jar), so unlike Furnace/IC2 we cannot mutate in place: each affected recipe is rebuilt with the canonical `getMainItemStack` and replaced **at its index** (`List.set`) preserving count + order — never `Iterator.remove`, never a removal (BB-3). Built on a new shared `OutputRewriter.rewriteList(list, view, resolve)` list-backing of the existing core (`mapItems`/`isChanged` helpers shared with `rewriteOutputs`). Crusher keeps public non-final `secondaryOutput`/`secondaryChance` (no `UniCrusherRecipe` subclass); Metal Press iterates the `ArrayListMultimap` by mold key via `ListMultimap.get(key)`; Arc furnace preserves `specialRecipeType` (upstream dropped it); Metal Press preserves a non-default `inputSize`. Dropped upstream's `getFirstEntry` input lookup + `uniques` dedup (they existed only because upstream removed+re-added; input rewriting = M5-deferred). Tests: 2 new `OutputRewriterTest` cases (`rewriteList` count-preserving + unchanged-identity) + existing 4 green; `./gradlew test build` + Spotless (auto-applied)/Checkstyle green. **T3 gate VERIFIED (2026-08-13, GTNH pack run):** `[unidict-verify] summary: 164 passed, 0 failed`; `PASS integration=ie` lines — `machine=arcFurnace rewritten=12`, `machine=blastFurnace rewritten=2`, `machine=crusher rewritten=1`, `machine=metalPress rewritten=4`, `machines=4 rewritten=19` — match the runtime `IE Integration: rewrote outputs of 19 IE machine recipes`. The single `*FAIL` grep hit is a false positive (the word "failed" in `summary: … 0 failed`); strict `unidict-verify] FAIL ` = 0 matches. Prior lines unchanged (Furnace 465, IC2 69, AE2 98, `spikeA oredict-bridge nameToId=55317`, `resource report resources=145`). No UniDict exception/MixinApplyError (remaining IC2 `reactorMOX` ParseExceptions + `ClassNotFoundException`s are pre-existing non-UniDict pack noise).
- [x] Chest `…integration=Chest` — **COMPLETE (2026-08-13).** Loot rewrite (`ChestIntegration`, "best-effort, minor"): rewrites the ITEM of every Forge chest-loot entry (each `WeightedRandomChestContent` the game can drop from dungeons/mineshafts/villages/strongholds/bonus chests) in place to the canonical `getMainItemStack` — non-destructive (BB-3): only the entry's item reference changes via the accessor seam, so the `contents` list keeps its exact count/order and each entry keeps its weights/min/max. Introduces the plan's **first accessor seam** (`ChestGenHooksMixin` @Accessor&times;2: static `chestInfo` registry + instance `contents`), plus a **documented 1.7.10-necessitated second accessor** (`WeightedRandomChestContentMixin`): upstream (1.12.2) rewrote `theItemId` as a *public* field directly — in MC 1.7.10 it is `private` (notch `qx.b`, SRG `field_76297_b`, verified via fields.csv + javap), so we expose get+set via a second @Accessor mixin (remap=**true**, vanilla field; the ChestGenHooks fields are remap=false Forge-added). Interfaces `IChestGenHooksAccessor` + `IWeightedRandomChestContentAccessor` (plain, rule 6); fakes in test sources. Registered early via `Mixins.CHEST_GEN`; wired in `IntegrationModule`. Tests: 3 `ChestIntegrationTest` (fake-driven `rewriteContents`/`rewriteCategory`/`rewriteContent`) green; `./gradlew test build` + Spotless/Checkstyle green (16 suites, 0 fail). Runs at POST_INIT (default), early-skips on empty resource model; config-gated `Config.chest()`. **Toolchain catches (AP vs runtime):** (1) the Mixin **AP** requires an `@Accessor` stub to return the *exact* field descriptor (`HashMap`/`ArrayList`, not `Map`/`List`) or it fails `Could not locate @Accessor target …`; (2) the Mixin **transformer** rejects a *non-abstract instance* `@Accessor` at apply time (`InvalidAccessorException: @Accessor method accessor$contents is not abstract` — the run-1 7:50 PM crash while applying `ChestGenHooksMixin`), so every instance accessor is `protected abstract`, while the static `chestInfo` accessor stays a concrete `private static` method (the proven M3 `OreDictionaryMixin` pattern — Java forbids `abstract static`). **T3 gate VERIFIED (2026-08-13, GTNH pack run):** clean boot (no crash; the 7:50 PM `ChestGenHooksMixin` apply crash is fixed); `[unidict-verify] PASS integration=Chest rewritten=38`; `[unidict-verify] summary: 165 passed, 0 failed` (IE run was 164; the new `integration=Chest` line is the only delta); prior lines unchanged (IC2 machines=10 rewritten=69, AE2 rewritten=98, IE machines=4 rewritten=19, `spikeA oredict-bridge nameToId=55317`, `resource report resources=145`); runtime `Chest Integration: rewrote 38 chest loot entries to their canonical items`.

## M7 — Machine rewrites (accessor/mixin): EIO · Railcraft · TE (Forestry removed)
- [x] **Source: EIO** `…integration=EIO` — `EIOIntegration` (alloy smelter + SAG mill via `OutputRewriter.rewriteList`, `List.set`) + `OreDictionaryPreferencesMixin`/`IOreDictionaryPreferencesAccessor` (clears EIO's `preferences` so it yields the canonical entry). Registered early via `Mixins.ENDER_IO` + `TargetMods.ENDER_IO`, wired in `IntegrationModule` (`Config.enderIO() && Loader.isModLoaded`). **Design note:** the two EIO `OutputView`s are lazy `Supplier`s so `EIOIntegration.<clinit>` never resolves Ender IO's classes — required so the package-private seams stay T2-reachable on a test classpath without Ender IO (see §EIO below). Tests: 4 `EIOIntegrationTest` green (fake-driven `rewriteRecipes` seam + `fixOreDictPreferences` via `FakeOreDictionaryPreferencesAccessor`).
- [x] **Source: Railcraft** `…integration=Railcraft` — `RailcraftIntegration` (blast furnace via `OutputRewriter.rewriteList`, `List.set`) + `BlastFurnaceCraftingManagerMixin`/`IBlastFurnaceCraftingManagerAccessor` (private `recipes` list). Registered via `Mixins.RAILCRAFT` + `TargetMods.RAILCRAFT`, wired in `IntegrationModule`. Tests: 2 `RailcraftIntegrationTest` green (fake-driven `rewriteRecipes` seam via `FakeBlastFurnaceCraftingManagerAccessor`).
- [x] **Source: TE** `…integration=ThermalExpansion` — `TEIntegration` (redstone furnace / pulverizer / induction smelter via `OutputRewriter.rewriteOutputs`, `Map.setValue`; rebuilt through the M0-Spike-B `@Invoker`s via the `IRecipeFurnaceFactory`/`IRecipePulverizerFactory`/`IRecipeSmelterFactory` interface seams) + 3 accessor mixins (`FurnaceManagerMixin`/`PulverizerManagerMixin`/`SmelterManagerMixin`, each `recipeMap` `private static` `@Accessor`) + 3 accessor interfaces. Runs at `@SpecifiedLoadStage(LOAD_COMPLETE)`. Registered via `Mixins.THERMAL_EXPANSION`, wired in `IntegrationModule`. Tests: 2 `TEIntegrationTest` green (fake-driven `rewriteOutputs` seam via `FakeFurnaceManagerAccessor`/`FakePulverizerManagerAccessor`/`FakeSmelterManagerAccessor`).
- [x] **T2 tests green** — 5 accessor fakes + 3 integration test classes (EIO 4 / Railcraft 2 / TE 2) all pass (`./gradlew test` = 82 tests, 0 failures; Spotless/Checkstyle clean). The EIO `fixOreDictPreferences` seam was widened to accept `IOreDictionaryPreferencesAccessor` for testability (mirrors Chest's `rewriteCategory(accessor, …)`).
- [x] **T3 in-game verify — EIO ✅, Railcraft ✅, TE BROKEN then fixed.** A full `runClient` (all kept mods) confirmed the EIO and Railcraft verify lines PASS, but **TE failed fatally**: `LoaderException: NoClassDefFoundError: …mixins.late.TERecipeFactory` / `IllegalClassLoadError: … is in a defined mixin package … cannot be referenced directly`. The wrapper `TERecipeFactory` lived in the closed mixin package `mixins.late` and could not be referenced from the non-mixin `TEIntegration`. **Fix:** surfaces the TE `@Invoker`s through the interface-on-target pattern exactly like the accessors — each `@Invoker` mixin now implements an `IRecipe*Factory` interface (in `com.mrfuzzihead.unidict.te`, non-mixin) and `TEIntegration` casts the recipe value object to it; the `@Invoker` is invoked inside the mixin package (legal). `TERecipeFactory` deleted. **Re-run `runClient` to reconfirm `PASS integration=TE machines=3` now CONFIRMED — 2026-08-14 M9 regression: `machines=3 rewritten=253`, no `NoClassDefFoundError`/`IllegalClassLoadError`.**

## M8 — API/helper surface — MOSTLY DEFERRED (~)
- [ ] Only the minimal read surface kept integrations actually use

## M9 — Cleanup sweep + full regression
- [x] **Sweep:** no dead-code stragglers remain in `src/main` — a grep for `FixedSizeList`/`Dependencies`/`DependenceWatcher`/`Instantiator`/`searchForModules`/`@Module`/`getField`/`setField`/removed-mod ids hits only javadoc/comments describing their removal, never live code
- [x] **Galacticraft stub note** present: `// TODO: Galacticraft integration (deferred stub).` in `IntegrationModule` (javadoc); Galacticraft dep left `compileOnly`-commented in `dependencies.gradle`
- [x] `./gradlew build` green: 19 test classes / **79 tests, 0 failures** (incl. `DeterminismGuardTest`); Spotless + Checkstyle clean
- [x] **Full T3 regression (2026-08-14, full dev mod set):** `runClient -PunidictDevVerify` → `[unidict-verify] summary: 124 passed, 0 failed`; 0 `unidict-verify] FAIL` matches; 0 mixin/fatal matches. Per-integration all PASS: IC2 `machines=10 rewritten=75`, AE2 `rewritten=61`, Chest `rewritten=38`, EIO `machines=2 rewritten=0`, Railcraft `rewritten=0`, TE `machines=3 rewritten=253`; `spikeA oredict-bridge nameToId=15489`; resource report `resources=105`. (*Evidence path: `build/runclient-log.txt`; the `[unidict-verify]` lines went to the console appender, not `run/client/logs/latest.log` — see note below.*)
- [x] **Eyeball pass:** only errors in the run are cosmetic AE2 block-texture PNG warnings (missing assets, pre-existing) — no UniDict or mixin errors
- [x] `git log --oneline` reads as the milestone list: `… M0→M2→M4→M6→M7 complete` (M3/M5 landed within those commits)

## Build-better track (`PLAN.md` has detail + gates)
- [ ] **BB-1 Transparency** — `/unidict report`: per-resource canonical entry, variants, owners, what got rewritten
- [ ] **BB-2 Config presets** — grouped categories; minimal / standard / max-compat
- [ ] **BB-3 Non-destructive rewriting** — outputs-only rewrites; grep guard; fabricated-map T2 test
- [ ] **BB-4 Broader equivalence** — ≥1 non-OD equivalence class implemented, tested, reported
  - **Scope decision (2026-08-13):** fuels/coke is the first non-metal equivalence class — **deferred but prioritized (next build-better milestone, not M6/M7).** Motivated by the pack genuinely wanting single stackable coal coke (Railcraft + IE both emit `fuelCoke`). Engine is already resource-agnostic (`getMainItemStack`), so this needs (1) a new kind for the fuel/coke form in the M1 taxonomy (within the 64-kind guard), (2) a config surface (resurrect the deferred `customUnifiedResources` / a "fuels" preset toggle), and (3) a **fuel-equivalence classifier** that preserves per-item burn time / exact-item-check semantics — NOT naive OD tag-equality (metals are near-interchangeable; fuels are not). Prior to implementation, verify in the live GTNH pack that Railcraft/IE coke actually share a tag and what each fuel value is. Add a verify/report line + T3 gate (BB-4 gate: "≥1 non-OD equivalence class implemented, tested, reported").
- [~] Reload / re-run module — deferred
